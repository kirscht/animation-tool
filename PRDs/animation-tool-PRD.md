Generate / update single-file html '~/Desktop/animation-tool.html'

- Create a little html tool that allows me to make small minimalistic animations that:
    - canvas should have stretch bars
    - allows drawing, frame-by-frame
    - has a play/pause button
    - allows export to animated gif file
    - allows the animation to be saved using the file picker
    - allows saved animations to be opened using the file picker
    - allows the canvas to be cleared to clear all frames
    - allow a frame to be cleared on the canvas
    - allows shapes to be pulled onto the canvas
        - allows the shapes to be resized on the canvas
        - for line shapes, can be stretched from either end of the line
        - allows color selection of shapes and lines on the canvas
    - allows drawing on the canvas
    - allows undo of previous operations

- also generate / update a Go (Go Lang) executable '~/Desktop/animation-tool' with the same functionality. that can run on Windows, Linux, and MacOS